<Window x:Class="ReinstallSys.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReinstallSys" 
        xmlns:usercontrol="clr-namespace:ReinstallSys.UserController"
        mc:Ignorable="d"
        Title="MainWindow">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <usercontrol:MyStepBar Grid.Row="0" x:Name="myStepBar"/>

        
           
        <!--<Viewbox Grid.Row="1" Stretch="Fill">
            <Canvas x:Name="mainContent" Height="320" Width="520">
                
            </Canvas>
        </Viewbox>-->
        <UniformGrid x:Name="mainContent" Grid.Row="1">
            
        </UniformGrid>

    </Grid>
</Window>
